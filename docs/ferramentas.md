# Ferramentas

Light: Download + OCR expõe 1 ferramenta (todas somente leitura).

### 1. `contas_light_download_ocr_consultar`
**Input**: `login`, `senha`, `codigo_cliente` (opcional), `codigo_instalacao` (opcional), `mes` (opcional)

Light: Download + OCR, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Light: Download + OCR
```
